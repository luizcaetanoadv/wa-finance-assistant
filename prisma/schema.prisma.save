datasource db {
  provider = "postgresql"
  url      = "COLE_A_EXTERNAL_DATABASE_URL_AQUI?sslmode=require"
}
datasource db {
  provider = "postgresql"
  url      = "COLE_A_EXTERNAL_DATABASE_URL_AQUI?sslmode=require"
}
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model GoogleAuth {
  id           String   @id @default(cuid())
  userKey      String   @unique
  accessToken  String
  refreshToken String?
  scope        String?
  tokenType    String?
  expiryDate   BigInt?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
